# [PackageDev] target_format: plist, ext: tmLanguage
---
name: TaskJuggler
scopeName: source.tjp3
fileTypes: [tjp3]
# Arbitrary UUID, until I figure out where this is used.
uuid: 24341012-fbff-4468-926e-348ed6563de2

macros:
  ID: (?:[a-zA-Z_][a-zA-Z0-9_]*)
  capturedID: ({{ID}})
  stringDoubleQuoted: \"(?:[^\"\\]|\\.)+\"
  stringSingleQuoted: \'(?:[^\'\\]|\\.)+\'
  capturedStringSingleLine: ({{stringDoubleQuoted}}|{{stringSingleQuoted}})

patterns:
- include: '#account'

repository:
  account:
    patterns:
    # Order matters: Multi-line pattern must preceded the single line version.
    - begin: '\s*(account)\s+(?:{{capturedID}}\s+)?{{capturedStringSingleLine}}\s*\{'
      end: '\}'
      beginCaptures:
        '1': { name: storage.type.account.tjp3 }
        '2': { name: entity.name.type.account.tjp3 }
        '3': { name: string.quoted.tjp3 }
      patterns:
        - match: '^\s*(flags)\s(?:({{ID}})(?:\s*,\s*({{ID}}))*)?'
          captures:
            '1': { name: storage.type.flags.tjp3 }
            '2': { name: entity.name.type.flags.tjp3 }
            '3': { name: entity.name.type.flags.tjp3 }
    - match: '\s*(account)\s+(?:{{capturedID}}\s+)?{{capturedStringSingleLine}}'
      captures:
        '1': { name: storage.type.account.tjp3 }
        '2': { name: entity.name.type.account.tjp3 }
        '3': { name: string.quoted.tjp3 }
